"""Language-specific scanner plugins.

Each plugin implements the LanguageScanner protocol defined in `plugins.base`.
Plugins are registered explicitly in `core.registry` to avoid import-time surprises.
"""
