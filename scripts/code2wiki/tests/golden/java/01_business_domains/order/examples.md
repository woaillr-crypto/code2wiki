<!-- GENERATED BY AUTO-WIKI SCANNER -->
# order 常见变更指南

## 根据代码结构推断的常见变更场景

### 新增/修改 API 接口

- 参考现有 Controller: `OrderController` (`src/main/java/com/example/order/controller/OrderController.java`)
- 业务逻辑放入 Service: `OrderService` (`src/main/java/com/example/order/service/OrderService.java`)
- 复用现有 DTO、校验、权限和异常处理风格

### 新增 MQ 消费者

- 参考现有 Consumer: `OrderCreatedConsumer` (`src/main/java/com/example/order/mq/OrderCreatedConsumer.java`)
- 实现 `RocketMqHandle` 接口
- 在 `RocketMqHandleFactory` 中注册 Tag 路由
- 在 Tag 枚举中新增常量
- 设计幂等键和失败补偿策略

### 新增/修改数据字段

- 修改 Entity: `OrderEntity` (`src/main/java/com/example/order/entity/OrderEntity.java`)
- 检查 Mapper/Repository: `OrderRepository` (`src/main/java/com/example/order/repository/OrderRepository.java`)
- 处理历史数据默认值和兼容性

## 典型需求记录模板

每次完成需求后建议在此补充：

- **需求**：
- **涉及业务概念**：
- **修改的文件**：
- **风险点**：
- **测试建议**：
