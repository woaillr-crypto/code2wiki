<!-- GENERATED BY AUTO-WIKI SCANNER -->
# order Workflows

## 基于代码结构推断的流程线索

以下是从 Controller/Service/MQ Consumer 的 public 方法签名中提取的候选流程，需要结合源码确认真实业务语义。

### OrderController (入口 API) — `src/main/java/com/example/order/controller/OrderController.java`

> 订单 API 入口，处理客户端下单与查询请求。

| 方法 | 返回类型 | 参数 |
| --- | --- | --- |
| create | String | @RequestBody String payload |
| get | String | @PathVariable Long id |
| list | String |  |

### OrderService (业务服务) — `src/main/java/com/example/order/service/OrderService.java`

> 订单核心业务服务：负责创建订单、查询订单。

| 方法 | 返回类型 | 参数 |
| --- | --- | --- |
| create | String | String payload |
| get | String | Long id |

### OrderCreatedConsumer (消息消费) — `src/main/java/com/example/order/mq/OrderCreatedConsumer.java`

| 方法 | 返回类型 | 参数 |
| --- | --- | --- |
| onMessage | void | String body |

## 流程文档模板

对于每个核心流程，建议补充：

- **触发方式**：API/MQ/Job/回调
- **参与角色**：用户/系统/外部服务
- **前置条件**：状态、权限、参数
- **主流程**：步骤 1 → 2 → 3
- **状态变化**：从 X 到 Y
- **数据写入**：哪些表/字段
- **外部调用**：Dubbo/Feign/MQ 发送
- **失败分支**：回滚/补偿/告警
- **幂等与重试**：如何处理重复
